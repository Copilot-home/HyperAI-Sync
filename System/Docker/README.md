# Docker Desktop

Machine: Apple Silicon / `darwin/arm64`

Docker CLI path:

- Config: `/Users/cuongnguyen/.docker/config.json`
- Daemon config: `/Users/cuongnguyen/.docker/daemon.json`
- Desktop settings: `/Users/cuongnguyen/Library/Group Containers/group.com.docker/settings-store.json`

Current exported snapshots:

- `/Users/cuongnguyen/Workspace/System/Docker/docker-cli-config.json`
- `/Users/cuongnguyen/Workspace/System/Docker/docker-daemon-config.json`

Current CLI context:

- `desktop-linux`

Current daemon status:

- Docker CLI is installed and reports `darwin/arm64`.
- Docker Desktop daemon is not currently reachable from `docker ps`; start `/Applications/Docker.app` before running containers.

Notes:

- Docker Desktop app is installed at `/Applications/Docker.app`.
- Local project compose files are under `/Users/cuongnguyen/Workspace/Projects`.
- Installer cache was removed from the active workspace because Docker is already installed.
